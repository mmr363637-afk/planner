# راهنمای نسخهٔ ۱٫۸

## مسیرهای استفاده

| قابلیت | مسیر |
|---|---|
| دربارهٔ اپ و سازنده | بالای تنظیمات ← «دربارهٔ این اپ»؛ نام سازنده، تلگرام و تغییرات نسخه |
| پیشنهاد کار بعدی | بالای خانه؛ زمان/انرژی، دلیل، شروع، پیشنهاد دیگر |
| روز منعطف و آینده | خانه یا برنامه ← برنامه‌ها ← «روز منعطف، سناریوها و اصلاح تخمین زمان» |
| اصلاح تخمین | تب «یادگیری از زمان واقعی» در کارگاه |
| جزوه و کارت مستند | مرور ← «جزوهٔ متصل به منبع» |
| پاک‌کردن مرورهای برنامه‌ریزی‌شده | مرور ← تب مرور؛ دکمهٔ پاک‌کردن زیر نمودار بار مرور |
| تمرین علت مشترک | مرور ← اشتباهات؛ حداقل دو اشتباه با علت مشترک و پاسخ |
| نسخهٔ ایمنی | تنظیمات ← وضعیت ذخیره و تاریخچه |
| تعارض ابری | تنظیمات ← Supabase ← مقایسه و دریافت از ابر |
| بکاپ‌های مستقل درایو | تنظیمات ← Google Drive ← دیدن ۳۰ نسخهٔ اخیر |

## محدودیت‌های صریح

- پیشنهادها قواعد محلیِ قابل‌توضیح‌اند، نه مدل هوش مصنوعی. برآورد آمادگی یک شاخص تجربی است، نه احتمال قبولی. تعداد تست و پوشش مباحث دیده می‌شود.
- سناریوها فقط کارهای در انتظارِ بازهٔ انتخابی (حداکثر ۹۰ روز) را بازچینی می‌کنند. کلاس‌ها، روزهای مجاز، کار فعال، مهلت امتحان/برنامه و توالی فازها رعایت می‌شود. اگر کار در ظرفیت جا نشود، اعمال غیرفعال است: ظرفیت/استراحت/مهلت را اصلاح یا تسک بزرگ را دستی خرد کنید. موعد مرورهای مستقل/کارت‌ها تغییر نمی‌کند و بار آن‌ها جداگانه هشدار داده می‌شود.
- اصلاح تخمین زمان حداقل سه کارِ یادگیریِ کامل با زمان واقعی می‌خواهد؛ میانهٔ نسبت‌ها در بازهٔ ۰٫۵ تا ۲ استفاده می‌شود. کارهای قبلاً استفاده‌شده دوباره وارد محاسبه نمی‌شوند. فقط تخمین مباحث برای برنامه‌های بعدی تغییر می‌کند؛ برنامهٔ فعلی دست‌نخورده است.
- هدف ۲۰ دقیقه‌ای یا مدت پیشنهاد، هدف نمایش‌داده‌شدهٔ جلسه است؛ تایمر را خود کاربر پایان می‌دهد، نه ثبت خودکارِ مطالعهٔ انجام‌نشده.
- منبع حداکثر ۱۵MB، ۱۰۰ صفحه و ۵۰۰هزار نویسه است؛ حداکثر ۳۰ منبع با مجموع ۲میلیون نویسه نگه داشته می‌شود. فایل اصلی ذخیره نمی‌شود؛ متن بررسی‌شده و شمارهٔ صفحه ذخیره می‌شود.
- PDF باید لایهٔ متنی داشته باشد؛ PDF اسکن‌شده پیام راهنما دارد و تصویر صفحه را می‌توان جداگانه OCR کرد. استخراج PDF فارسی ممکن است ترتیب خطوط را دقیق بازسازی نکند. متن و OCR پیش از ذخیره قابل اصلاح هستند.
- OCR فارسی/انگلیسی با Tesseract در مرورگر اجرا می‌شود. موتور و دادهٔ فارسی/انگلیسی همراه خروجی سایت ارائه می‌شوند (حدود ۷MB، فقط هنگام استفاده). دریافت اولیه اینترنت می‌خواهد اما وابسته به CDN خارجی نیست. پس از کش‌شدن فایل‌ها، استفادهٔ مجدد آفلاین ممکن است؛ باقی‌ماندن کش تضمین نیست. تصویر برای تشخیص متن به سرور ارسال نمی‌شود. OCR در worker جداگانه اجرا می‌شود و قابل لغو است؛ پس از سه دقیقه متوقف می‌شود و پیشنهاد تصویر کوچک‌تر می‌دهد. در دستگاه کم‌توان ممکن است کند باشد. حالت کاملاً آفلاینِ اولین OCR وعده داده نمی‌شود.
- کارت خودکار از ساختار «عنوان: تعریف» و «سؤال؟ پاسخ» استخراج می‌شود؛ برای متن آزاد، نقل‌قول دستی در دسترس است. پاسخ ابداع نمی‌شود. صحت علمی منبع نیاز به بررسی انسان دارد. ویرایش پاسخ کارت در ویرایشگر عمومی، ارجاع قبلی آن را حذف می‌کند تا شاهد نادرست باقی نماند.
- تمرین علت مشترک بر اساس علت‌های خوداظهاریِ یکسان است؛ تشخیص علمیِ علت پنهان یا تضمین رفع ضعف نیست. نتیجه، خودارزیابی روی سؤال‌های قبلی است؛ سؤال تازه برای سنجش انتقال یادگیری پیشنهاد می‌شود.
- تاریخچهٔ محلی تا ۸ نسخه با بودجهٔ حدود ۱۶MB نگه می‌دارد (آخرین نسخه همیشه حفظ می‌شود). با پاک‌کردن دادهٔ سایت حذف می‌شود. فایل بکاپ بیرون دستگاه همچنان لازم است.
- شروع دانلود فایل به معنی ذخیرهٔ موفق آن نیست؛ UI «درخواست دانلود» را ثبت می‌کند و بررسی فایل بر عهدهٔ کاربر است.
- Drive یک بکاپ دستیِ نسخه‌دار است، نه ادغام زنده. فایل‌های قدیمی حذف خودکار نمی‌شوند و از سهمیهٔ Drive استفاده می‌کنند؛ انتخاب از ۳۰ نسخهٔ اخیر ممکن است.

## پاک‌کردن برنامهٔ مرور

در تب «مرور»، دکمهٔ «پاک‌کردن مرورهای برنامه‌ریزی‌شده» تعداد و محدودهٔ حذف را نشان می‌دهد. همهٔ مرورهای فاصله‌دارِ انجام‌نشده، شامل عقب‌افتاده، امروز و آینده، در محدودهٔ نمایش‌داده‌شده حذف می‌شوند. اگر از پیشنهادِ یک مبحث وارد شده‌اید، حذف هم فقط همان مبحث را در بر می‌گیرد؛ برای حذف کل برنامه ابتدا «همهٔ مباحث» را بزنید.

گزینهٔ حذف کارهای «مرور» انجام‌نشده از برنامهٔ مطالعه به‌صورت پیش‌فرض روشن است و می‌توانید آن را خاموش کنید. کارهای یادگیری، مرورهای انجام‌شده، سابقهٔ مطالعه، فلش‌کارت‌ها و اشتباهات حذف نمی‌شوند. این دکمه ساخت مرورهای جدید پس از مطالعه‌های بعدی را غیرفعال نمی‌کند.

پیش از حذف، نسخهٔ ایمنی در تاریخچه ذخیره می‌شود. با شکست ذخیره، تغییر هم‌زمان داده‌ها یا وجود جلسهٔ فعال، حذف انجام نمی‌شود. بازیابی از «تنظیمات ← وضعیت ذخیره و تاریخچه» ممکن است؛ توجه کنید بازیابی، **کل وضعیت نسخهٔ انتخابی** را برمی‌گرداند، نه فقط مرورها.

## Supabase: اقدام لازم صاحب پروژه

1. از داده‌های فعلی فایل بکاپ بگیرید.
2. برای پروژهٔ تازه، ابتدا `supabase/setup.sql` را اجرا کنید.
3. سپس **`supabase/safe-sync.sql`** را در SQL Editor همان پروژه اجرا کنید. تکرار اجرای آن ایمن است.
4. این مهاجرت ستون revision و جدول ۸ نسخهٔ قبلی را اضافه می‌کند و نوشتن مستقیم کلاینت‌های قدیمی را می‌بندد. بنابراین دستگاه‌ها باید نسخهٔ ۱٫۸ اپ را دریافت کنند.
5. دستگاه دارای دادهٔ قدیمی ابتدا «مقایسه و دریافت از ابر» را انتخاب می‌کند. خواندن ابر به‌تنهایی مجوز بازنویسی نیست. کاربر پس از مشاهدهٔ دو نسخه، جایگزینی کامل را تأیید می‌کند؛ نسخهٔ کنارگذاشته‌شده در تاریخچهٔ محلی می‌ماند. ادغام خودکار انجام نمی‌شود.
6. کنترل CAS در یک تراکنش سمت سرور انجام می‌شود: دو نویسنده با نسخهٔ پایهٔ یکسان نمی‌توانند هر دو موفق شوند. نسخهٔ پایه همراه وضعیت همان دستگاه نگه داشته می‌شود، به دستگاه دیگر صادر نمی‌شود و به حساب و پروژه متصل است.
7. در تعارض ارسال خودکار متوقف می‌شود؛ پس از انتخاب نسخه می‌توان ارسال خودکار را دوباره روشن کرد. نبود مهاجرت، خطای روشن می‌دهد و هیچ fallback به upsert ناامن وجود ندارد.

مهاجرت در محیط محلی با PostgreSQL/WASM (PGlite) از نظر اجرا، تکرارپذیری، کنترل تعارض، محدودیت تاریخچه، ممنوعیت نوشتن مستقیم و RLS بین کاربران آزموده می‌شود. **این معادل اجرای آن روی حساب Supabase زنده یا تست واقعی OAuth دو دستگاه نیست.** هیچ مهاجرت یا انتشار زنده‌ای در این تغییرات خودکار انجام نشده است.

## تست و ساخت

```bash
npm ci
npm run typecheck
npm run build
npm test
npm run dev -- --host 0.0.0.0
```

`docs/` خروجی قابل انتشار GitHub Pages است؛ تست build-freshness وجود قابلیت‌ها در آن را کنترل می‌کند. نسخهٔ کش PWA به v10 ارتقا یافته است.

تست‌های جدید شامل تصمیم‌گیری و ماتریس، زمان‌بندی غیرمخرب، دادهٔ ناکافی آمادگی، استخراج و صحت نقل‌قول، مهاجرت/تاریخچه، مسیرهای UI، خطاهای همگام‌سازی و خود SQL هستند. هیچ تستی به پروژهٔ ابری زنده متصل نمی‌شود.

## نتیجهٔ بررسی این تغییرات — ۲۰۲۶/۰۹/۲۲

- TypeScript و ساخت production موفق؛ **۴۵۴ تست در ۵۹ فایل** پاس شدند. `npm audit` پس از ارتقای وابستگی‌ها **صفر آسیب‌پذیری شناخته‌شده** گزارش کرد؛ این جایگزین ممیزی امنیتی مستقل نیست.
- نمایش یک‌بارهٔ «چی جدیده؟» پس از ارتقا، حفظ وضعیت دیده‌شدن پس از بازخوانی، ورود به درباره از تنظیمات، نام و لینک تلگرام و مشاهدهٔ دوبارهٔ تغییرات نسخه با تست UI بررسی شدند.
- هفت تست اضافه برای پاک‌کردن مرورها: محدودهٔ حذف، حفظ داده‌های نامرتبط، ذخیره و بازیابی تاریخچه، فیلتر مبحث، خطای ذخیره، تغییر هم‌زمان، جلسهٔ فعال و تأیید/انصراف در UI؛ همچنین وجود دکمه در خروجی نهایی کنترل می‌شود.
- Chromium با اندازهٔ موبایل: راه‌اندازی با هدف ۱۳۵ دقیقه، ساخت برنامه، پیش‌نمایش سناریو، تأیید کارت مستند، حفظ داده پس از بازخوانی؛ بدون دانلود ناخواسته، خطای جاوااسکریپت یا اسکرول افقی در مسیر آزموده‌شده.
- خروجی production زیر مسیر `/planner/`: PDF متنی دوخطی واقعاً استخراج شد؛ ذخیرهٔ نسخهٔ محلی تأیید شد. این آزمون جامع همهٔ چیدمان‌های PDF فارسی نیست.
- Chromium دسکتاپ/تاریک: جابه‌جایی Tab داخل دیالوگ، Escape و بازگشت فوکوس، رد پیشنهاد و شروع پیشنهاد بعدی با مبحث درست و هدف ۲۵ دقیقه؛ بازخوانی آفلاین با حفظ جلسهٔ فعال.
- OCR در محیط توسعه و production: استخراج واقعی تصویر انگلیسی/فارسی، لغو هنگام شروع و استخراج دوباره پس از قطع اینترنت با فایل‌های کش‌شده موفق بود. در نمونهٔ فارسی، «پایه» نادرست تشخیص داده شد؛ مرحلهٔ بازبینی ضروری است. هیچ ادعایی دربارهٔ دقت عمومی OCR یا کارایی گوشی ضعیف نداریم.
- SQL در PostgreSQL/WASM محلی: اجرای مجدد مهاجرت، نویسندگان هم‌زمان، تعارض revision، نگه‌داری ۸ نسخه، جلوگیری از نوشتن مستقیم و جداسازی کاربران با RLS آزموده شد.
- ارتباط واقعی چند دستگاه با Supabase، OAuth/Drive واقعی، اجرای SQL در پروژهٔ زنده و انتشار سایت انجام نشده‌اند. آزمون انسانی نیز هنوز انجام نشده است.
- ساخت هنوز دربارهٔ چانک‌های بزرگ هشدار می‌دهد؛ موتورهای PDF/OCR و داده‌های آماده جداگانه بارگیری می‌شوند، اما این تغییرات ادعای بهینه‌سازی کامل حجم برنامه ندارند.

## چک‌لیست پذیرش با کاربر واقعی

به کاربر هدف بدون توضیح شفاهی این کارها را بدهید:
1. از نصب تازه یک مطالعهٔ کوتاه شروع کند؛ زمان و تعداد کلیک تا شروع را یادداشت کنید.
2. هدف روزانهٔ متفاوت از ۴ ساعت انتخاب کند و از پیش‌نمایش برنامه بسازد؛ مقدار باید همان انتخابش باشد.
3. برای امروز ۳۰ دقیقه ظرفیت بدهد؛ پیش‌نمایش را بررسی و فقط پس از تأیید اعمال کند؛ تاریخچه باید نسخهٔ قبلی را داشته باشد.
4. یک PDF متنی و یک تصویر فارسی وارد کند، متن را بازبینی کند، یک کارت را تأیید و در مرور نقل‌قولش را باز کند.
5. نتیجه‌ای را که در جزوه نیست جست‌وجو کند؛ نباید پاسخ حدسی بگیرد.
6. پس از مهاجرت Supabase، دو دستگاه آفلاین/آنلاین را با تغییرات متفاوت امتحان کند؛ تعارض باید انتخاب صریح بخواهد و نسخهٔ دیگر قابل بازیابی بماند.

ثبت مکث‌ها و اشتباه‌ها از پرسیدن «خوشت آمد؟» مهم‌تر است. این تست انسانی هنوز انجام نشده است.

## نمایش تغییرات پس از انتشار

پس از merge و تکمیل موفق انتشار GitHub Pages، نسخهٔ جدید قابل دریافت است. در PWA/تب دارای کش قدیمی ممکن است لازم باشد اعلان به‌روزرسانی تأیید یا اپ دوباره باز شود؛ merge به‌تنهایی به معنی انتشار آنی روی همهٔ دستگاه‌ها نیست.

وقتی نسخهٔ جدید واقعاً بارگیری شود، پنجرهٔ «چی جدیده؟» برای کاربری که راه‌اندازی اولیه را تمام کرده و این نسخه را ندیده، هنگام بازشدن اپ نمایش داده می‌شود. با بستن آن، نسخهٔ دیده‌شده ذخیره می‌شود و در بازشدن بعدی تکرار نمی‌شود. برای مشاهدهٔ دوباره: تنظیمات ← دربارهٔ این اپ ← تغییرات نسخهٔ فعلی. کسی که همین نسخهٔ ۱٫۸ را قبلاً در همان محیط دیده، اعلان خودکار تکراری نمی‌گیرد.
