# تغییرات برنامه‌ریز مطالعه

## 1.8.0 — تصمیم روشن و ذخیرهٔ امن (2026-09-22)

- دکمهٔ پاک‌کردن مرورهای برنامه‌ریزی‌شده: نمایش محدوده و تعداد، تأیید صریح، انتخاب حذف کارهای مرور، نسخهٔ ایمنی و حفظ مرورهای انجام‌شده/فلش‌کارت‌ها.
- پیشنهاد توضیح‌پذیر کار بعدی، انتخاب وقت و انرژی، رد پیشنهاد و بازگردانی روزانه.
- شروع کوتاه ۲۰ دقیقه‌ای و پیش‌نمایش مستقیم برنامه از تنظیمات اولیه؛ ویزارد مستقل نیز هدف روزانه را رعایت می‌کند.
- چیدمان مینیمال خانه با حفظ چیدمان سفارشی قبلی؛ نمایش تدریجی آمار و جمع‌شدن دستاوردها.
- کارگاه روز منعطف و سه سناریوی آینده، ظرفیت کلاس‌ها، مهلت‌ها، توالی فازها و فاصلهٔ مرور؛ بدون حذف کارهای جا‌نشده.
- یادگیری تخمین زمان از میانهٔ حداقل سه کار تکمیل‌شده؛ شواهد پذیرفته‌شده دوباره اعمال نمی‌شوند.
- جزوهٔ متنی/PDF/OCR تصویر با بازبینی، کارت‌های مبتنی بر نقل‌قول و جست‌وجوی عین متن؛ بدون مدل زبانی یا پاسخ حدسی.
- موتور OCR و مدل‌های فارسی/انگلیسی همراه سایت، worker قابل لغو با محدودیت زمان؛ بدون وابستگی به CDN خارجی.
- ارتقای PDF.js و Vite و وابستگی‌های آسیب‌پذیر شناسایی‌شده در npm audit.
- تمرین اشتباه‌های دارای علت مشترک، پاسخ پنهان، ثبت خودارزیابی و مرور بعدی.
- رفع خطای ماتریس؛ شفافیت شاخص آمادگی و دادهٔ ناکافی؛ ساعت‌های معمول مطالعه به‌جای ادعای تمرکز.
- حذف دانلود ناخواستهٔ بکاپ، تاریخچهٔ نسخه‌های محلی و پشتیبان قبل از جایگزینی/بازچینی.
- ترجیح IndexedDB جدیدتر بر آینهٔ قدیمی پس از پرشدن سهمیه؛ توقف نوشتن تا پایان خواندن اولیه.
- بکاپ‌های غیرقابل‌بازنویسی Drive و انتخاب نسخه؛ RPC اتمیک CAS، RLS و تاریخچهٔ سروری Supabase.
- مهاجرت Supabase آماده است اما روی سرور زنده اجرا نشده؛ جزئیات در UPGRADE_GUIDE.md.

## 1.7.1 — Google Drive واقعی + سوییچ سرویس سینک (2026-09-21)

### ✨ تازه‌ها
- **گوگل درایوِ واقعاً-کارآمد:** Client ID ساختگی حذف شد؛ کارت درایو حالا راهنمای مرحله‌به‌مرحله‌ی دریافت Client ID رایگان از Google Cloud را داخل خودش دارد + اعتبارسنجی فرم + ترجمه‌ی خطاهای رایج OAuth (invalid_client، access_denied، بلاک پاپ‌آپ، origin ثبت‌نشده) به فارسی.
- **سوییچ سرویس سینک:** در تنظیمات ← داده‌ها، بین «Google Drive» (پیش‌فرض) و «Supabase» یکی را انتخاب می‌کنی؛ هر دو کدپیاده‌سازی‌شده و زنده‌اند. اتصال مجدد درایو بعد از اولین رضایت بی‌صدا انجام می‌شود.

## 1.7.0 — سینک ابری واقعی ☁️ + آپدیتِ بی‌درسَر (2026-09-21)

### ✨ تازه‌ها
- **همگام‌سازی ابری Supabase:** شناسه‌ی ناشناس خودکار (Anonymous Sign-In) — بدون هیچ فرم/پسوردی سینک راه می‌افتد؛ «اتصال ایمیل» اختیاری برای دائمی‌کردن حساب و ورود روی دستگاه‌های دیگر با کد یک‌بارمصرف. ذخیره‌ی خودکار (debounce) + push/pull دستی در تنظیمات. دیتابیس متعلق به خودِ کاربر با RLS (`supabase/setup.sql`) — جایگزین دکمه‌ی گوگل درایو که با Client ID ساختگی هرگز کار نمی‌کرد و حذف شد.

### 🐞 رفع باگ
- **«بعد از آپدیت بخش‌ها خراب می‌شن»:** بعد از فعال‌شدن سرویس‌ورکر تازه، اپ یک‌بار خودش را رفرش می‌کند (`lib/appHealth.ts` + گاردهای sessionStorage) و پاک‌سازی کش قدیمی با تأخیر انجام می‌شود تا تب‌های زامبی فرصت رفرش داشته باشند؛ شکستِ لود چانک lazy هم به‌جای کرش تب، یک رفرشِ امن را راه می‌اندازد و ErrorBoundary در این حالت پیامِ راهنما + دکمه‌ی «رفرش و رفع مشکل» نشان می‌دهد.
- **گیر‌کردن بی‌نهایت «در حال آماده‌سازی بانک منتخب‌ها…»:** حالا با backoff خودکار (۵ تلاش) دوباره تلاش می‌کند و در صورت شکست، بنرِ قابل‌اقدام با دکمه‌ی «تلاش دوباره» نشان می‌دهد؛ کارت‌های شخصی همیشه بدون وقفه دیده می‌شوند.

### 🛠 فنی
- `src/lib/supabaseSync.ts` (پیکربندی، هویت ناشناس، push/pull JSONB با updated_at) + `src/components/SupabaseSyncCard.tsx`
- `src/lib/appHealth.ts` (تشخیص خطای چانک چندمرورگره + guardedReload)
- حذف `googleDrive.ts`/`GoogleDriveSyncCard.tsx` و تست‌هایش (فیلد `settings.googleDrive` برای سازگاری خواندن داده‌ی قدیمی باقی است)
- SW `v9`: claim-اول + پاک‌سازی تأخیری کش‌های قدیمی

## 1.6.0 — حالت جنگی 🏴‍☠️ (2026-09-13)

همه‌چیز مثل همیشه کاملاً آفلاین و روی خود دستگاه؛ بدون سرور و بدون سینک.

### ✨ تازه‌ها
- **حالت جنگی (Cram Mode):** «تا امتحان چند ساعت مونده؟» — موتور تازه‌ی `lib/cram` از اشتباه‌های سررسیده، مرورها، مباحث ضعیف/نخوانده و کارت‌های سررسیده، بلاک‌های ۵۰ دقیقه کار + ۱۰ دقیقه استراحت می‌چیند؛ با پیش‌نمایش زنده، نوار پیشرفت و دکمه‌ی ▶ شروع مستقیم جلسه روی هر مبحث. (صفحه‌ی امتحانات + بنر خانه)
- **بایگانی درس‌ها 📦:** درس تمام‌شده از فهرست‌های فعال و برنامه‌های تازه پنهان می‌شود ولی آمارش در گزارش‌ها می‌ماند؛ با بخش «درس‌های بایگانی‌شده» و برگرداندن یک‌تایی.
- **تکثیر برنامه 📋:** هر برنامه را با یک کلیک (همراه تسک‌هایش، با پیشرفت صفرشده) کپی کن.
- **جستجوی سراسری قوی‌تر:** پالت فرمان (Ctrl+K) حالا یادداشت‌های مباحث، دفترچه‌ی اشتباهات و ژورنال را هم می‌گردد.
- **نمودار برنامه در برابر واقعیت 🎯:** در آمار، ۷ روز اخیرِ برنامه‌ریزی‌شده در برابر انجام‌شده با درصد تحقق.
- **حافظه‌سنج 💽:** در تنظیمات → داده‌ها، فضای واقعی استفاده‌شده‌ی اپ روی دستگاه دیده می‌شود.
- **دو دستاورد تازه:** «اولین نبرد ⚔️» و «فاتح جنگ 🏆».

### ⚡️ کارایی (مخصوص گوشی)
- تیک صفحه‌ی مطالعه (۵۰۰ms) و بنر سراسری (۱s) فقط وقتی جلسه «در حال اجرا»ست رندر می‌کنند و فقط همان بخش‌های زنده را — نه کل درخت.
- شمارش نشان مرورها و متادیتای مدیا (MediaSession) کش شدند و فقط هنگام تغییر به‌روز می‌شوند.
- موتور صوتی محیطی از باندل بوت جدا شد (لیزی‌لود) و صف ذخیره‌سازی هنگام بستن صفحه فلاش می‌شود تا چیزی گم نشود.

### 🛠 فنی
- `src/lib/cram.ts` موتور خالص و تست‌پذیر + `src/lib/cram.test.ts` (۷ تست).
- `Subject.archived` و `AppState.cram` با اعتبارسنجی هنگام بازیابی در `stateIO`.

## 1.5.0
- بچ آفلاین: فریز تداوم و فروشگاه XP، خروجی ICS، ایمپورت CSV کلوز، گزارش ماهانه، اتاق مطالعه با انتقال QR، گزارش هفتگی.
