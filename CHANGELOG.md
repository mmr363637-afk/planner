# تغییرات برنامه‌ریز مطالعه

## 1.7.0 — پایدارتر، سریع‌تر، کامل‌تر 🚀 (2026-09-14)

همه‌چیز مثل همیشه کاملاً آفلاین و روی خود دستگاه؛ بدون سرور و بدون سینک.

### ✨ تازه‌ها
- **چاپ / PDFِ برنامه‌ی هفتگی 🖨️:** در صفحه‌ی جدول (برنامه ← جدول زمانی) دکمه‌ی «چاپ / PDF» — فقط یک برگِ تمیز A4 چاپ می‌شود: هفت روزِ هفته با مشغله‌های ثابت (کلاس و…) و کارهای مطالعه‌ی برنامه‌ریزی‌شده‌ی همین هفته (با ☐ برای تیک زدن). از دیالوگِ چاپِ مرورگر «Save as PDF» هم می‌شود PDF ساخت.
- **خروجی CSV (اکسل/گوگل‌شیت) 📊:** در تنظیمات ← داده‌ها — «خروجی CSV ثبت تست‌ها» (تاریخ؛درس؛مبحث؛کل سؤالات؛درست؛درصد) و «خروجی CSV جلسات مطالعه» (تاریخ؛درس؛مبحث؛دقیقه؛حالت؛ارزیابی؛حواس‌پرتی). با BOM و جداکننده‌ی `;` — مستقیم در اکسلِ فارسی باز می‌شود.
- **همگام‌سازی چندتب 🔄:** اگر تبِ دیگرِ مرورگر داده را عوض کند (خودش mirror را می‌نویسد)، تبِ این طرف با رویداد `storage` خبر می‌گیرد: بدون جلسه‌ی فعال، بی‌درنگ همگام می‌شود؛ با جلسه‌ی فعال، تایمر از بین نمی‌رود و فقط بنر «🔄 داده در تبِ دیگر به‌روز شد» نمایش داده می‌شود و همگام‌سازی با پایان جلسه انجام می‌گیرد.

### ⚡️ کارایی (مخصوص گوشی)
- **تقسیمِ پک‌های منتخب:** ۵۹۸ پک/۸٬۷۷۱ کارت از یک فایلِ ۳۸۰KB‌ای به ۲۸ فایلِ درس‌به‌درس تقسیم شد (باندل بوت ~۹۶KB). متادیتای سبک همگام می‌ماند (فهرست و شمارنده‌ها) و کارت‌های هر پک فقط با بازکردن همان مبحث، به‌صورت تنبل لود می‌شوند (با کش + همگام‌سازی درخواست‌های هم‌زمان).

### 🛠 فنی
- **CI + دپلوی خودکار:** `.github/workflows/ci-deploy.yml` — روی هر PR: تایپ‌چک + ۳۶۰+ تست + بیلد + **E2E با Playwright** (سه جریان کاربری در مرورگر واقعی روی بیلدِ production). روی push به main: بیلدِ `docs/` خودکار کامیت و دپلوی می‌شود (حالت branch publishing GitHub Pages؛ خروجی deterministic است و حلقه‌ی بی‌نهایت ندارد).
- **دسترسی‌پذیری:** `Modal` حالا `role=dialog` + `aria-modal` + فوکوسِ خودکار روی پنل است؛ `Button`/`IconButton` برچسب `aria-label` می‌گیرند (از `title` یا صریح)؛ توست‌ها در `role=status aria-live=polite`؛ حلقه‌ی `:focus-visible` سراسری در `index.css`؛ ۵ دکمه‌ی آیکونیِ بدون برچسب (پخش/مکث، ضبط، ±۳۰ دقیقه) برچسب گرفتند.
- **بغل پک‌های تکراری:** ۶ پکی که هم‌id با پکِ درسِ دیگر داشتند (نسخه‌ی cross-map شده‌ی یک مبحث) — نسخه‌ی قدیمی به `#2` رینیم شد و نسخه‌ی مازاد حذف شد؛ ۵۹۸ id منحصربه‌فرد.
- `importCuratedCards` همگام شد (`Promise<number>` + `ensurePackById`)؛ `DeckDetail` کارت‌های پک را با `ensurePacks` لود تنبل می‌کند و تا آماده‌شدن بنر «در حال آماده‌سازی کارت‌های منتخب…» نشان می‌دهد.
- `e2e/flows.spec.ts` + `playwright.config.ts` (webServer روی `vite preview`، viewport موبایل، locale `fa-IR`).

## 1.6.0 — حالت جنگی 🏴‍☠️ (2026-09-13)

همه‌چیز مثل همیشه کاملاً آفلاین و روی خود دستگاه؛ بدون سرور و بدون سینک.

### ✨ تازه‌ها
- **حالت جنگی (Cram Mode):** «تا امتحان چند ساعت مونده؟» — موتور تازه‌ی `lib/cram` از اشتباه‌های سررسیده، مرورها، مباحث ضعیف/نخوانده و کارت‌های سررسیده، بلاک‌های ۵۰ دقیقه کار + ۱۰ دقیقه استراحت می‌چیند؛ با پیش‌نمایش زنده، نوار پیشرفت و دکمه‌ی ▶ شروع مستقیم جلسه روی هر مبحث. (صفحه‌ی امتحانات + بنر خانه)
- **بایگانی درس‌ها 📦:** درس تمام‌شده از فهرست‌های فعال و برنامه‌های تازه پنهان می‌شود ولی آمارش در گزارش‌ها می‌ماند؛ با بخش «درس‌های بایگانی‌شده» و برگرداندن یک‌تایی.
- **تکثیر برنامه 📋:** هر برنامه را با یک کلیک (همراه تسک‌هایش، با پیشرفت صفرشده) کپی کن.
- **جستجوی سراسری قوی‌تر:** پالت فرمان (Ctrl+K) حالا یادداشت‌های مباحث، دفترچه‌ی اشتباهات و ژورنال را هم می‌گردد.
- **نمودار برنامه در برابر واقعیت 🎯:** در آمار، ۷ روز اخیرِ برنامه‌ریزی‌شده در برابر انجام‌شده با درصد تحقق.
- **حافظه‌سنج 💽:** در تنظیمات → داده‌ها، فضای واقعی استفاده‌شده‌ی اپ روی دستگاه دیده می‌شود.
- **دو دستاورد تازه:** «اولین نبرد ⚔️» و «فاتح جنگ 🏆».

### ⚡️ کارایی (مخصوص گوشی)
- تیک صفحه‌ی مطالعه (۵۰۰ms) و بنر سراسری (۱s) فقط وقتی جلسه «در حال اجرا»ست رندر می‌کنند و فقط همان بخش‌های زنده را — نه کل درخت.
- شمارش نشان مرورها و متادیتای مدیا (MediaSession) کش شدند و فقط هنگام تغییر به‌روز می‌شوند.
- موتور صوتی محیطی از باندل بوت جدا شد (لیزی‌لود) و صف ذخیره‌سازی هنگام بستن صفحه فلاش می‌شود تا چیزی گم نشود.

### 🛠 فنی
- `src/lib/cram.ts` موتور خالص و تست‌پذیر + `src/lib/cram.test.ts` (۷ تست).
- `Subject.archived` و `AppState.cram` با اعتبارسنجی هنگام بازیابی در `stateIO`.

## 1.5.0
- بچ آفلاین: فریز تداوم و فروشگاه XP، خروجی ICS، ایمپورت CSV کلوز، گزارش ماهانه، اتاق مطالعه با انتقال QR، گزارش هفتگی.
